# Local builds of the wrapper (go build -o coredns ./cmd/coredns) and the
# artifacts the architect go-build job persists (<binary>-<os>-<arch>, cosign
# bundles, .platforms), plus the generated Makefile's outputs.
/coredns
/coredns-*
/coredns-warnlist-plugin
/coredns-warnlist-plugin-*
*.bundle
/.platforms
/.ldflags

# Tools people drop into the checkout.
/nancy-fixer

# Local experiment data, never committed.
/abusech-hosts.txt
/domains.txt
/Corefile
/Corefile cm
